"""Агент-роутер для распределения запросов между специализированными агентами."""

from app.config.llm_factory import create_llm

LOCATION = "global"
LLM = "gemini-2.5-flash"

# LLM для роутера с автоматическим логированием
llm_flash = create_llm(
    model=LLM,
    location=LOCATION,
    temperature=0,
)

ROUTER_STAGE_INSTRUCTION = """Твоя задача — проанализировать последние сообщения пользователя и определить, к какому агенту направить запрос.
Если в сообщении пользователя есть число и это число до 100 (включительно), направь запрос на "morning". Если число больше 100, направь запрос на "evening".
Доступные маршруты:
- "morning" — утренний агент
- "evening" — вечерний агент
Во всех остальных случаях направь на "morning".
Верни ТОЛЬКО одно слово из списка выше (в нижнем регистре, без кавычек и дополнительных символов).
"""

