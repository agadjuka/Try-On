ssh -i ssh-key-2026-04-15.key ubuntu@Да, план отличный! Сделай docker-compose.yml с двумя сервисами: `bot` и `api`. 

Но у нас есть два важных требования, чтобы всё было максимально просто и заработало с фронтендом на Vercel:

1. **CORS:** Обязательно убедись, что в файле `main.py` (FastAPI) подключен и настроен `CORSMiddleware`, разрешающий запросы с любых доменов (`allow_origins=["*"]`), иначе браузер заблокирует запросы от Vercel. Покажи, какой код добавить в main.py, если его там нет.

2. **Проблема HTTPS:** Vercel работает по HTTPS, поэтому браузер не даст отправить запрос на голый `http://IP:8080`. Я не хочу сейчас покупать домен и настраивать Nginx/SSL вручную. 
Давай добавим в `docker-compose.yml` третий сервис — **Cloudflared (Cloudflare Tunnel)** или **Ngrok** (что на твой взгляд надежнее для бесплатного использования). 
Мне нужно, чтобы этот туннель пробрасывал трафик на наш сервис `api` (порт 8080) и выдавал наружу публичный HTTPS URL, который я просто вставлю в настройки Vercel.

Напиши обновленный `docker-compose.yml` с этими тремя сервисами и инструкцию, как мне получить эту HTTPS ссылку после деплоя.

логи
cd ~/bot_project
docker-compose logs --tail=100 bot