services:
  bot:
    container_name: vyon-bot
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env
    environment:
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/gcp-sa.json
    volumes:
      - ./secrets/gcp-sa.json:/run/secrets/gcp-sa.json:ro
      - bot_data:/code/data

volumes:
  bot_data:
